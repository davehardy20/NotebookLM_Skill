# NotebookLM Skill Environment Configuration

# Security: Encryption key for browser state storage
# IMPORTANT: Change this to a secure, unique 32+ character key
# This key encrypts your Google authentication cookies/tokens
# If lost, you will need to re-authenticate
STATE_ENCRYPTION_KEY=change-this-to-a-secure-32-char-key!

# Optional: Custom data directory (defaults to ~/.claude/skills/notebooklm/data)
# NOTEBOOKLM_DATA_DIR=/custom/path/to/data

# Optional: Custom browser state directory
# NOTEBOOKLM_BROWSER_STATE_DIR=/custom/path/to/browser_state

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info

# Optional: Node environment (development, production, test)
# NODE_ENV=development
